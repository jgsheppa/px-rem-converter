import React from 'react';

import AvisioLogoWithText from '../AvisioLogoWithText';

import { HeaderContainer, HeaderInnerContainer } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderInnerContainer>
        This will eventually be the header
        {/* <AvisioLogoWithText /> */}
      </HeaderInnerContainer>
    </HeaderContainer>
  );
};

export default Header;
